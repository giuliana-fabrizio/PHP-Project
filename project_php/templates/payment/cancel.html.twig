{% extends 'base.html.twig' %}

{% block title %}Paiement échoué{% endblock %}

{% block body %}
    {{ parent() }}

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">Paiement échoué</div>

                    <div class="card-body">
                        <p>Malheureusement, nous n'avons pas pu traiter votre paiement.</p>

                        <p>Veuillez vérifier vos informations de paiement et réessayer.</p>

                        <div class="text-center mt-4">
                            <a href="{{ path('app_pay_event', {'id': event.id}) }}" class="btn btn-primary">Réessayer le Paiement</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
